"""Interactive Telegram UI layer for GIGVEYRO. All mutating actions call
into the same repository/service classes as the authenticated web API -
this package never computes balances, eligibility, or payout amounts on
its own."""
